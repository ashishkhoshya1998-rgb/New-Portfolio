---
import Layout from '../../layouts/Layout.astro';
import TopNav from '../../components/TopNav';
import FooterCTA from '../../components/FooterCTA.astro';
import { Image } from 'astro:assets';
import { getCollection, render } from 'astro:content';
import { decks } from '../../data/decks';

export async function getStaticPaths() {
  const projects = await getCollection('projects');
  return projects.map((project) => ({
    params: { slug: project.id },
    props: { project },
  }));
}

const { project } = Astro.props;
const { Content } = await render(project);
const { title, subtitle, description, role, services, year, accentColor, coverImage, nextProject, parentProject } = project.data;
const isLocked = Astro.url.pathname.includes('memoir');
const hasDeck = !!decks[project.id];
---

<Layout title={`${title} — Ashish Khoshya`} description={description}>
  <TopNav currentPath="/projects" client:load />

  {isLocked && (
    <div class="pw-gate" id="pw-gate">
      <div class="pw-gate__card">
        <div class="pw-gate__icon">
          <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
            <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
          </svg>
        </div>
        <h2 class="pw-gate__title" id="pw-title">This project is protected</h2>
        <p class="pw-gate__desc" id="pw-desc">Enter the password to view this case study.</p>
        <form class="pw-gate__form" id="pw-form" aria-labelledby="pw-title" aria-describedby="pw-desc">
          <label for="pw-input" class="sr-only">Password</label>
          <input
            type="password"
            class="pw-gate__input"
            id="pw-input"
            placeholder="Password"
            autocomplete="off"
            aria-describedby="pw-error"
            aria-invalid="false"
          />
          <button type="submit" class="pw-gate__btn" data-fluid-cs><span class="pw-gate__btn-highlight"></span><span class="pw-gate__btn-label">Unlock</span></button>
        </form>
        <p class="pw-gate__error" id="pw-error" role="alert" aria-live="polite">Incorrect password. Try again.</p>
        <a href="/projects" class="pw-gate__back" data-fluid-cs><span class="pw-gate__back-highlight"></span><span class="pw-gate__back-label"><span aria-hidden="true">←</span> Back to Projects</span></a>
      </div>
    </div>
  )}

  <main id="main-content" class="cs" style={`--csAccentRaw:${accentColor};`}>
    {parentProject && (
      <div class="cs__back-bar">
        <div class="content-wrap">
          <a href={`/project/${parentProject.slug}`} class="cs__back-link">
            <span aria-hidden="true">←</span> Back to {parentProject.title}
          </a>
        </div>
      </div>
    )}

    <!-- Hero — full viewport with morphing cover image -->
    <section class="cs__hero" data-cs-hero>
      {coverImage && (
        <>
          {typeof coverImage === 'string' ? (
            <img
              src={coverImage}
              alt=""
              class="cs__hero-cover"
              aria-hidden="true"
            />
          ) : (
            <Image
              src={coverImage}
              alt=""
              class="cs__hero-cover"
              aria-hidden="true"
              format="avif"
              quality={80}
              widths={[640, 960, 1280, 1920]}
              sizes="100vw"
            />
          )}
          <div class="cs__hero-cover-gradient" />
        </>
      )}
      <div class="cs__hero-overlay">
        <div class="cs__hero-left">
          <h1 class="cs__hero-title">{title}</h1>
          <p class="cs__hero-subtitle">{subtitle}</p>
        </div>
        <div class="cs__hero-right">
          <div class="cs__hero-meta-item">
            <span class="cs__hero-meta-label">Role</span>
            <span class="cs__hero-meta-value">{role}</span>
          </div>
          <div class="cs__hero-meta-divider" />
          <div class="cs__hero-meta-item">
            <span class="cs__hero-meta-label">Services</span>
            <span class="cs__hero-meta-value">{services.join(' · ')}</span>
          </div>
          <div class="cs__hero-meta-divider" />
          <div class="cs__hero-meta-item">
            <span class="cs__hero-meta-label">Year</span>
            <span class="cs__hero-meta-value">{year}</span>
          </div>
        </div>
      </div>
      <div class="cs__hero-scroll-hint">Scroll down</div>
    </section>

    {hasDeck && (
      <div class="cs__deck-prompt content-wrap">
        <a href={`/deck/${project.id}`} class="cs__deck-prompt-inner" data-astro-reload>
          <div class="cs__deck-prompt-left">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2" /><path d="M8 21h8" /><path d="M12 17v4" /></svg>
            <div class="cs__deck-prompt-copy">
              <span class="cs__deck-prompt-title">View the slide deck</span>
              <span class="cs__deck-prompt-text">Get the highlights in 2 minutes</span>
            </div>
          </div>
          <span class="cs__deck-prompt-arrow">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </span>
        </a>
      </div>
    )}

    <!-- MDX Body with section nav + share sidebar -->
    <div class="cs__content-layout">
      <nav class="cs__section-nav" aria-label="Case study sections" id="cs-section-nav">
        <span class="cs__section-nav-title">Summary</span>
        <div class="cs__section-nav-items">
          <span class="cs__section-nav-highlight"></span>
          <!-- Populated by JS from h2 headings -->
        </div>
      </nav>
      <article class="content-wrap cs__body">
        <Content />
      </article>
    </div>

    <!-- Next Project / All Projects -->
    <section class="cs__next">
      <div class="content-wrap cs__next-inner">
        {nextProject && (
          <>
            <span class="cs__next-label">Next Project</span>
            <a href={`/project/${nextProject.slug}`} class="cs__next-link">
              {nextProject.title}
              <span class="cs__next-arrow" aria-hidden="true">→</span>
            </a>
          </>
        )}
        <a href="/projects" class="cs__all-projects">View all projects <span aria-hidden="true">→</span></a>
      </div>
    </section>

    <div id="contact">
      <FooterCTA />
    </div>
  </main>
</Layout>

<style>
  /* Derive themed accent — darkened in light mode for contrast */
  .cs {
    --csAccent: var(--csAccentRaw);
  }
  :global([data-theme='light']) .cs {
    --csAccent: color-mix(in srgb, var(--csAccentRaw), black 40%);
  }

  /* ── Password gate ── */
  .pw-gate {
    position: fixed;
    inset: 0;
    z-index: 9999;
    background: var(--bg);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px;
  }

  .pw-gate__card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
    max-width: 360px;
    width: 100%;
    text-align: center;
  }

  .pw-gate__icon {
    color: var(--text-muted);
    opacity: 0.4;
    margin-bottom: 8px;
  }

  .pw-gate__title {
    font-family: var(--font-display);
    font-size: 24px;
    font-weight: 600;
    color: var(--text);
    margin: 0;
  }

  .pw-gate__desc {
    font-size: 14px;
    color: var(--text-muted);
    margin: 0;
    opacity: 0.7;
  }

  .pw-gate__form {
    display: flex;
    gap: 8px;
    width: 100%;
    margin-top: 8px;
  }

  .pw-gate__input {
    flex: 1;
    padding: 12px 16px;
    border-radius: var(--radius-full);
    border: 1px solid var(--border);
    background: var(--overlay-soft);
    color: var(--text);
    font-size: 14px;
    font-family: inherit;
    outline: none;
    transition: border-color 0.2s;
  }

  .pw-gate__input:focus {
    border-color: var(--accent);
  }

  .pw-gate__btn {
    position: relative;
    overflow: hidden;
    padding: 12px 24px;
    border-radius: var(--radius-full);
    border: 1px solid var(--accent);
    background: transparent;
    color: var(--accent);
    font-size: 14px;
    font-weight: 600;
    font-family: inherit;
    cursor: none;
    transition: color 0.25s ease, border-color 0.25s ease;
    white-space: nowrap;
  }

  .pw-gate__btn-highlight {
    position: absolute;
    inset: 0;
    border-radius: var(--radius-full);
    background: var(--accent);
    opacity: 0;
    pointer-events: none;
    z-index: 0;
    clip-path: circle(0% at 50% 50%);
  }

  .pw-gate__btn-label {
    position: relative;
    z-index: 1;
  }

  .pw-gate__btn:hover {
    color: var(--btn-hover-text);
  }

  .pw-gate__error {
    font-size: 13px;
    color: var(--accent-red);
    margin: 0;
    opacity: 0;
    transition: opacity 0.2s;
  }

  .pw-gate__error.visible {
    opacity: 1;
  }

  .pw-gate__back {
    position: relative;
    overflow: hidden;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    font-size: 13px;
    color: var(--text-muted);
    text-decoration: none;
    padding: 6px 14px;
    border-radius: 8px;
    transition: color 0.2s;
    margin-top: 8px;
    cursor: none;
  }

  .pw-gate__back-highlight {
    position: absolute;
    inset: 0;
    border-radius: 8px;
    background: var(--overlay-soft);
    opacity: 0;
    pointer-events: none;
    z-index: 0;
    clip-path: circle(0% at 50% 50%);
  }

  .pw-gate__back-label {
    position: relative;
    z-index: 1;
    display: inline-flex;
    align-items: center;
    gap: 4px;
  }

  .pw-gate__back:hover {
    color: var(--text);
  }

  /* ── Back bar ── */
  .cs__back-bar {
    position: relative;
    z-index: 1;
    padding: 16px 0;
    margin-top: 72px;
    border-bottom: 1px solid var(--border);
  }

  .cs__back-link {
    font-size: 13px;
    font-weight: 500;
    color: var(--csAccent);
    text-decoration: none;
    transition: opacity 0.2s;
    cursor: none;
  }

  .cs__back-link:hover {
    opacity: 0.7;
  }

  /* ── Content layout with section nav ── */
  .cs__content-layout {
    position: relative;
  }

  .cs__section-nav {
    position: fixed;
    left: 32px;
    top: 50%;
    transform: translateY(-50%);
    z-index: 50;
    display: flex;
    flex-direction: column;
    gap: 0;
    max-width: 200px;
  }

  .cs__section-nav-title {
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--text-muted);
    margin-bottom: 12px;
    padding-left: 18px;
    opacity: 0.5;
  }

  .cs__section-nav :global(.cs__section-nav-items) {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .cs__section-nav :global(.cs__section-nav-highlight) {
    position: absolute;
    left: -8px;
    top: 0;
    height: 0;
    width: calc(100% + 16px);
    border-radius: 8px;
    background: color-mix(in srgb, var(--csAccent) 12%, transparent);
    opacity: 0;
    pointer-events: none;
    z-index: 0;
    clip-path: circle(0% at 50% 50%);
  }

  .cs__section-nav :global(.cs__section-nav-item) {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 8px;
    margin-left: -8px;
    border-radius: 8px;
    text-decoration: none;
    cursor: none;
    transition: opacity 0.2s;
  }

  .cs__section-nav :global(.cs__section-nav-item:hover) {
    opacity: 1 !important;
  }

  .cs__section-nav :global(.cs__section-nav-dot) {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--text-muted);
    flex-shrink: 0;
    transition: background 0.3s, transform 0.3s;
    opacity: 0.3;
  }

  .cs__section-nav :global(.cs__section-nav-item--active .cs__section-nav-dot) {
    background: var(--csAccent);
    transform: scale(1.4);
    opacity: 1;
  }

  .cs__section-nav :global(.cs__section-nav-label) {
    font-size: 12px;
    font-weight: 400;
    color: var(--text-muted);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    transition: color 0.2s, font-weight 0.2s;
    opacity: 0.5;
  }

  .cs__section-nav :global(.cs__section-nav-item--active .cs__section-nav-label) {
    opacity: 1;
    color: var(--text);
  }

  .cs__section-nav :global(.cs__section-nav-item--active .cs__section-nav-label) {
    color: var(--csAccent);
    font-weight: 600;
  }

  @media (max-width: 1200px) {
    .cs__section-nav {
      display: none;
    }
  }

  /* ── Hero — full viewport ── */
  .cs__hero {
    position: relative;
    height: 80vh;
    display: flex;
    align-items: flex-end;
    overflow: hidden;
  }

  .cs__hero-cover {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    pointer-events: none;
    will-change: transform;
  }

  .cs__hero-cover-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, var(--bg) 0%, var(--gradient-mid) 40%, var(--gradient-light) 70%, transparent 100%);
    pointer-events: none;
  }

  .cs__hero-overlay {
    position: relative;
    z-index: 2;
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    padding: 0 48px 48px;
    gap: 48px;
    opacity: 0;
    transform: translateY(16px);
    animation: csReveal 0.6s var(--ease-out-expo) 0.3s forwards;
  }

  .cs__hero-left {
    max-width: 600px;
  }

  .cs__hero-title {
    font-family: var(--font-display);
    font-size: clamp(40px, 6vw, 72px);
    font-weight: 700;
    color: var(--hero-text);
    line-height: 1.1;
    margin-bottom: 8px;
  }

  .cs__hero-subtitle {
    font-size: 16px;
    color: var(--hero-text-muted);
    line-height: 1.5;
  }

  /* Deck prompt — between hero and body */
  .cs__deck-prompt {
    padding-top: 32px;
    padding-bottom: 32px;
  }

  .cs__deck-prompt-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 24px 28px;
    text-decoration: none;
    border: 1px solid color-mix(in srgb, var(--csAccent, var(--accent)) 30%, transparent);
    border-radius: 12px;
    background: color-mix(in srgb, var(--csAccent, var(--accent)) 5%, var(--bg));
    transition: background 0.25s, border-color 0.25s;
    cursor: none;
  }

  .cs__deck-prompt-inner:hover {
    background: color-mix(in srgb, var(--csAccent, var(--accent)) 10%, var(--bg));
    border-color: color-mix(in srgb, var(--csAccent, var(--accent)) 50%, transparent);
  }

  .cs__deck-prompt-left {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .cs__deck-prompt-left > svg {
    color: var(--csAccent, var(--accent));
    flex-shrink: 0;
  }

  .cs__deck-prompt-copy {
    display: flex;
    flex-direction: column;
    gap: 3px;
  }

  .cs__deck-prompt-title {
    font-size: 15px;
    font-weight: 600;
    color: var(--text);
    font-family: var(--font-body);
  }

  .cs__deck-prompt-text {
    font-size: 13px;
    color: var(--text-muted);
    font-family: var(--font-body);
  }

  .cs__deck-prompt-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: color-mix(in srgb, var(--csAccent, var(--accent)) 15%, transparent);
    color: var(--csAccent, var(--accent));
    flex-shrink: 0;
    transition: background 0.25s, transform 0.25s;
  }

  .cs__deck-prompt-inner:hover .cs__deck-prompt-arrow {
    background: color-mix(in srgb, var(--csAccent, var(--accent)) 25%, transparent);
    transform: translateX(4px);
  }

  .cs__deck-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: var(--text);
    font-size: 13px;
    font-weight: 500;
    font-family: var(--font-body);
    text-decoration: none;
    transition: background 0.2s, border-color 0.2s, color 0.2s;
  }

  .cs__deck-btn:hover {
    background: var(--csAccent, #F5F0E8);
    color: var(--btn-hover-text);
    border-color: var(--csAccent, #F5F0E8);
  }

  .cs__deck-btn svg {
    opacity: 0.6;
    transition: transform 0.2s;
  }

  .cs__deck-btn:hover svg {
    opacity: 1;
    transform: translateX(2px);
  }

  .cs__hero-right {
    display: flex;
    flex-direction: column;
    gap: 16px;
    flex-shrink: 0;
  }

  .cs__hero-meta-item {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .cs__hero-meta-label {
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--csAccent);
  }

  .cs__hero-meta-value {
    font-size: 13px;
    color: var(--hero-meta);
    max-width: 200px;
  }

  .cs__hero-meta-divider {
    width: 100%;
    height: 1px;
    background: var(--hero-divider);
  }

  .cs__hero-scroll-hint {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    font-size: 11px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--hero-hint);
    opacity: 0;
    animation: csRevealHint 0.5s var(--ease-out-expo) 0.8s forwards;
  }

  @keyframes csReveal {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes csRevealHint {
    to {
      opacity: 1;
    }
  }


  /* ── MDX Body ── */
  .cs__body {
    max-width: var(--content-narrow);
    padding-top: 80px;
    padding-bottom: 80px;
  }

  /* h2 */
  .cs__body :global(h2) {
    font-family: var(--font-display);
    font-size: 36px;
    font-weight: 700;
    color: var(--text);
    line-height: 1.2;
    margin-top: 64px;
    margin-bottom: 24px;
  }

  .cs__body :global(h2:first-child) {
    margin-top: 0;
  }

  /* h3 */
  .cs__body :global(h3) {
    font-family: var(--font-display);
    font-size: 24px;
    font-weight: 700;
    color: var(--text);
    margin-top: 48px;
    margin-bottom: 16px;
  }

  /* p */
  .cs__body :global(p) {
    font-family: var(--font-body);
    font-size: 16px;
    color: var(--text-muted);
    line-height: 1.8;
    margin-bottom: 24px;
  }

  /* blockquote */
  .cs__body :global(blockquote) {
    position: relative;
    background: color-mix(in srgb, var(--csAccent) 5%, var(--card-bg));
    border-left: 3px solid var(--csAccent);
    border-radius: 0 var(--radius-md) var(--radius-md) 0;
    padding: 24px 28px;
    margin: 40px 0;
  }

  .cs__body :global(blockquote p) {
    font-family: var(--font-display);
    font-size: 17px;
    font-style: italic;
    color: var(--text);
    line-height: 1.7;
    margin-bottom: 0;
  }

  /* img — full-bleed breakout (skip ImageRow images) */
  .cs__body :global(img:not(.image-row__img)) {
    width: calc(100% + 200px);
    max-width: none;
    margin-left: -100px;
    border-radius: 12px;
    margin-top: 40px;
    margin-bottom: 40px;
    display: block;
    cursor: zoom-in;
  }

  /* Portrait / mobile screenshots — 0.6x scale on desktop */
  .cs__body :global(img.cs-portrait) {
    width: 200px;
    max-width: 100%;
    margin-left: auto;
    margin-right: auto;
    border-radius: 16px;
    box-shadow: 0 8px 40px var(--shadow-heavy);
  }

  /* video — full-bleed breakout (same as img) */
  .cs__body :global(video) {
    width: calc(100% + 200px);
    max-width: none;
    margin-left: -100px;
    border-radius: 12px;
    margin-top: 40px;
    margin-bottom: 40px;
    display: block;
  }

  /* strong */
  .cs__body :global(strong) {
    color: var(--text);
    font-weight: 600;
  }

  /* ul/ol */
  .cs__body :global(ul),
  .cs__body :global(ol) {
    padding-left: 24px;
    margin-bottom: 24px;
  }

  .cs__body :global(li) {
    font-size: 16px;
    color: var(--text-muted);
    line-height: 1.8;
    margin-bottom: 8px;
  }

  /* table — comparison cards */
  .cs__body :global(.table-wrap) {
    width: 100%;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    margin: 40px 0;
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    background: var(--card-bg);
  }

  .cs__body :global(table) {
    width: 100%;
    min-width: max-content;
    border-collapse: separate;
    border-spacing: 0;
    overflow: hidden;
  }

  .cs__body :global(thead) {
    background: color-mix(in srgb, var(--csAccent) 8%, var(--bg));
  }

  .cs__body :global(th) {
    font-family: var(--font-body);
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: var(--text);
    padding: 14px 16px;
    text-align: left;
    border-bottom: 1px solid var(--border);
  }

  .cs__body :global(th:first-child) {
    color: var(--text-muted);
  }

  .cs__body :global(td) {
    font-size: 14px;
    color: var(--text-muted);
    padding: 12px 16px;
    border-bottom: 1px solid var(--border);
    white-space: nowrap;
  }

  .cs__body :global(tr:last-child td) {
    border-bottom: none;
  }

  .cs__body :global(td:first-child) {
    font-weight: 500;
    color: var(--text-subtle);
    white-space: normal;
  }

  .cs__body :global(td:last-child) {
    color: var(--csAccent);
    font-weight: 600;
  }

  .cs__body :global(tr:hover td) {
    background: color-mix(in srgb, var(--csAccent) 3%, transparent);
  }

  /* ── Next Project ── */
  .cs__next {
    padding: 96px 0;
    border-top: 1px solid var(--border);
  }

  .cs__next-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 16px;
  }

  .cs__next-label {
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--text-muted);
  }

  .cs__next-link {
    font-family: var(--font-display);
    font-size: clamp(32px, 5vw, 56px);
    font-weight: 700;
    color: var(--text);
    text-decoration: none;
    line-height: 1.2;
    transition: color 0.2s;
    cursor: none;
  }

  .cs__next-link:hover {
    color: var(--csAccent);
  }

  .cs__next-link:hover .cs__next-arrow {
    transform: translateX(8px);
  }

  .cs__next-arrow {
    display: inline-block;
    margin-left: 12px;
    transition: transform 0.3s var(--ease-out-expo);
  }

  .cs__all-projects {
    font-size: 14px;
    font-weight: 500;
    color: var(--text-muted);
    text-decoration: none;
    margin-top: 24px;
    transition: color 0.2s;
    cursor: none;
  }

  .cs__all-projects:hover {
    color: var(--csAccent);
  }

  /* ── Mobile ── */
  @media (max-width: 768px) {
    .cs__hero {
      height: 60vh;
    }

    .cs__hero-overlay {
      flex-direction: column;
      align-items: flex-start;
      padding: 0 20px 24px;
      gap: 20px;
    }

    .cs__hero-title {
      font-size: 32px;
    }

    .cs__hero-right {
      flex-direction: row;
      flex-wrap: wrap;
      gap: 16px;
    }

    .cs__hero-meta-divider {
      display: none;
    }

    .cs__hero-scroll-hint {
      display: none;
    }

    .cs__body {
      padding-top: 48px;
      padding-bottom: 48px;
    }

    .cs__body :global(h2) {
      font-size: 26px;
      margin-top: 48px;
      margin-bottom: 16px;
    }

    .cs__body :global(h3) {
      font-size: 20px;
      margin-top: 36px;
    }

    .cs__body :global(p) {
      font-size: 15px;
      line-height: 1.75;
      margin-bottom: 18px;
    }

    .cs__body :global(img:not(.image-row__img)),
    .cs__body :global(video) {
      width: 100%;
      margin-left: 0;
      border-radius: 8px;
      cursor: zoom-in;
    }

    .cs__body :global(img.cs-portrait) {
      width: 160px;
      max-width: 60%;
      border-radius: 12px;
    }

    .cs__body :global(.table-wrap) {
      margin: 24px -20px;
      width: calc(100% + 40px);
      border-radius: var(--radius-md);
    }

    .cs__body :global(th),
    .cs__body :global(td) {
      padding: 10px 14px;
      font-size: 13px;
    }

    .cs__body :global(th) {
      font-size: 10px;
    }

    .cs__next {
      padding: 64px 0;
    }
  }

  /* ── Fullscreen Lightbox ── */
  :global(.cs-lightbox) {
    position: fixed;
    inset: 0;
    z-index: 9999;
    background: var(--lightbox-bg);
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 48px 8px 40px;
    opacity: 0;
    transition: opacity 0.3s ease;
    -webkit-user-select: none;
    user-select: none;
    overflow: auto;
    -webkit-overflow-scrolling: touch;
  }

  :global(.cs-lightbox--visible) {
    opacity: 1 !important;
  }

  :global(.cs-lightbox img) {
    max-width: 90vw;
    max-height: 90vh;
    width: auto;
    height: auto;
    object-fit: contain;
    border-radius: 8px;
    pointer-events: auto;
    opacity: 1 !important;
    transform: none !important;
  }

  :global(.cs-lightbox video) {
    width: 100%;
    height: auto;
    max-height: 80vh;
    object-fit: contain;
    border-radius: 4px;
    pointer-events: auto;
  }

  :global(.cs-lightbox__close) {
    position: fixed;
    top: 8px;
    right: 8px;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--overlay-hover);
    backdrop-filter: blur(8px);
    border: none;
    color: var(--hero-text);
    font-size: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: none;
    z-index: 10;
  }

  :global(.cs-lightbox__hint) {
    position: fixed;
    bottom: 12px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 11px;
    color: var(--hero-hint);
    letter-spacing: 1px;
    white-space: nowrap;
  }

  /* Invert dark-themed SVG diagrams in light mode */
  :global([data-theme='light']) .cs__body :global(img[src$=".svg"]) {
    filter: invert(1) hue-rotate(180deg);
  }
</style>

<script>
  // Password gate for locked projects
  (function() {
    const gate = document.getElementById('pw-gate');
    if (!gate) return;

    // Skip gate if already unlocked this session
    if (sessionStorage.getItem('ak-memoir-unlocked')) {
      gate.remove();
      return;
    }

    const form = document.getElementById('pw-form') as HTMLFormElement;
    const input = document.getElementById('pw-input') as HTMLInputElement;
    const error = document.getElementById('pw-error')!;
    const CORRECT = 'Alka@6146';

    document.body.style.overflow = 'hidden';
    // Make page behind inert while gate is visible
    const main = document.querySelector('main');
    if (main) main.setAttribute('inert', '');
    // Focus input without relying on autofocus attribute
    setTimeout(() => input.focus(), 0);

    form.addEventListener('submit', (e) => {
      e.preventDefault();
      if (input.value === CORRECT) {
        sessionStorage.setItem('ak-memoir-unlocked', '1');
        gate.style.opacity = '0';
        gate.style.transition = 'opacity 0.3s';
        document.body.style.overflow = '';
        if (main) main.removeAttribute('inert');
        setTimeout(() => gate.remove(), 300);
      } else {
        input.setAttribute('aria-invalid', 'true');
        error.classList.add('visible');
        input.value = '';
        input.focus();
        setTimeout(() => {
          error.classList.remove('visible');
          input.setAttribute('aria-invalid', 'false');
        }, 2000);
      }
    });
  })();

  import gsap from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);
  gsap.ticker.lagSmoothing(0);
  if (document.hidden) {
    const interval = setInterval(() => gsap.ticker.tick(), 16);
    document.addEventListener('visibilitychange', () => {
      if (!document.hidden) clearInterval(interval);
    }, { once: true });
  }

  // Cleanup on navigation
  document.addEventListener('astro:before-swap', () => {
    ScrollTrigger.getAll().forEach(t => t.kill());
  });

  function initSectionNav() {
    const nav = document.getElementById('cs-section-nav');
    if (!nav) return;

    const headings = document.querySelectorAll('.cs__body h2');
    if (!headings.length) return;

    // Keep the title and items wrapper, clear items
    const title = nav.querySelector('.cs__section-nav-title');
    let itemsWrap = nav.querySelector('.cs__section-nav-items') as HTMLElement;
    if (!itemsWrap) {
      nav.innerHTML = '';
      if (title) nav.appendChild(title);
      itemsWrap = document.createElement('div');
      itemsWrap.className = 'cs__section-nav-items';
      const hl = document.createElement('span');
      hl.className = 'cs__section-nav-highlight';
      itemsWrap.appendChild(hl);
      nav.appendChild(itemsWrap);
    } else {
      // Clear existing items but keep highlight
      const hl = itemsWrap.querySelector('.cs__section-nav-highlight');
      itemsWrap.innerHTML = '';
      if (hl) itemsWrap.appendChild(hl);
    }

    headings.forEach((h2, i) => {
      if (!h2.id) h2.id = `section-${i}`;

      const item = document.createElement('a');
      item.className = 'cs__section-nav-item';
      item.href = `#${h2.id}`;
      item.innerHTML = `
        <span class="cs__section-nav-dot"></span>
        <span class="cs__section-nav-label">${h2.textContent}</span>
      `;
      item.addEventListener('click', (e) => {
        e.preventDefault();
        h2.scrollIntoView({ behavior: 'smooth', block: 'start' });
      });
      itemsWrap.appendChild(item);
    });

    // Fluid highlight for section nav — cursor pours in
    const highlight = itemsWrap.querySelector('.cs__section-nav-highlight') as HTMLElement;
    if (highlight) {
      const navLinks = itemsWrap.querySelectorAll('.cs__section-nav-item');
      navLinks.forEach((link) => {
        link.addEventListener('mouseenter', (e: Event) => {
          const me = e as MouseEvent;
          const wrapRect = itemsWrap.getBoundingClientRect();
          const linkRect = link.getBoundingClientRect();
          const cursorX = me.clientX - wrapRect.left;
          const cursorY = me.clientY - wrapRect.top;

          highlight.style.top = (linkRect.top - wrapRect.top) + 'px';
          highlight.style.height = linkRect.height + 'px';
          highlight.style.clipPath = `circle(0% at ${cursorX}px ${cursorY}px)`;
          highlight.style.opacity = '1';

          requestAnimationFrame(() => {
            highlight.style.transition = 'clip-path 0.45s cubic-bezier(0.4, 0, 0.2, 1), top 0.35s cubic-bezier(0.4, 0, 0.2, 1), height 0.35s cubic-bezier(0.4, 0, 0.2, 1)';
            highlight.style.clipPath = `circle(150% at ${cursorX}px ${cursorY}px)`;
          });
        });
      });
      itemsWrap.addEventListener('mouseleave', (e: Event) => {
        const me = e as MouseEvent;
        const wrapRect = itemsWrap.getBoundingClientRect();
        const cx = me.clientX - wrapRect.left;
        const cy = me.clientY - wrapRect.top;
        highlight.style.transition = 'clip-path 0.35s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.15s ease 0.2s';
        highlight.style.clipPath = `circle(0% at ${cx}px ${cy}px)`;
        setTimeout(() => { highlight.style.opacity = '0'; }, 300);
      });
    }

    const navItems = nav.querySelectorAll('.cs__section-nav-item');

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const idx = Array.from(headings).indexOf(entry.target as Element);
            if (idx !== -1) {
              navItems.forEach((item, i) => {
                item.classList.toggle('cs__section-nav-item--active', i === idx);
              });
            }
          }
        });
      },
      { rootMargin: '-20% 0px -60% 0px' }
    );

    headings.forEach((h2) => observer.observe(h2));

    // Show/hide nav + share sidebar based on body scroll position
    const body = document.querySelector('.cs__body');
    if (body) {
      ScrollTrigger.create({
        trigger: body,
        start: 'top 20%',
        end: 'bottom 80%',
        onEnter: () => {
          gsap.to(nav, { opacity: 1, duration: 0.3 });
        },
        onLeave: () => {
          gsap.to(nav, { opacity: 0, duration: 0.3 });
        },
        onEnterBack: () => {
          gsap.to(nav, { opacity: 1, duration: 0.3 });
        },
        onLeaveBack: () => {
          gsap.to(nav, { opacity: 0, duration: 0.3 });
        },
      });
      nav.style.opacity = '0';
    }
  }


  function initHeroTransition() {
    const raw = sessionStorage.getItem('ak-project-rect');
    sessionStorage.removeItem('ak-project-rect');

    const heroCover = document.querySelector('.cs__hero-cover') as HTMLImageElement;
    const heroOverlay = document.querySelector('.cs__hero-overlay') as HTMLElement;
    const heroHint = document.querySelector('.cs__hero-scroll-hint') as HTMLElement;
    const heroGradient = document.querySelector('.cs__hero-cover-gradient') as HTMLElement;
    const hero = document.querySelector('.cs__hero') as HTMLElement;

    if (!heroCover || !hero) return;

    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (prefersReducedMotion) {
      if (heroOverlay) gsap.set(heroOverlay, { opacity: 1, y: 0 });
      if (heroHint) gsap.set(heroHint, { opacity: 1 });
      if (heroGradient) gsap.set(heroGradient, { opacity: 1 });
      return;
    }

    if (raw) {
      const rect = JSON.parse(raw);

      // Hide everything initially
      if (heroOverlay) {
        heroOverlay.style.opacity = '0';
        heroOverlay.style.transform = 'translateY(24px)';
        heroOverlay.style.animation = 'none';
      }
      if (heroHint) {
        heroHint.style.opacity = '0';
        heroHint.style.animation = 'none';
      }
      if (heroGradient) {
        heroGradient.style.opacity = '0';
      }

      function runAnimation() {
        // Position image at the stored card rect (fixed, above everything)
        gsap.set(heroCover, {
          position: 'fixed',
          top: rect.top,
          left: rect.left,
          width: rect.width,
          height: rect.height,
          objectFit: 'cover',
          zIndex: 100,
          borderRadius: 12,
          inset: 'auto',
        });

        // Animate image expanding to fill hero
        gsap.to(heroCover, {
          top: 0,
          left: 0,
          width: window.innerWidth,
          height: hero.offsetHeight,
          borderRadius: 0,
          duration: 0.8,
          ease: 'power3.out',
          onComplete: () => {
            gsap.set(heroCover, {
              clearProps: 'all',
            });
          },
        });

        // Fade in gradient
        if (heroGradient) {
          gsap.to(heroGradient, {
            opacity: 1,
            duration: 0.6,
            delay: 0.3,
          });
        }

        // Fade in overlay content
        if (heroOverlay) {
          gsap.to(heroOverlay, {
            opacity: 1,
            y: 0,
            duration: 0.5,
            ease: 'power3.out',
            delay: 0.5,
          });
        }
        if (heroHint) {
          gsap.to(heroHint, {
            opacity: 1,
            duration: 0.4,
            delay: 0.8,
          });
        }
      }

      // Wait for image to load if needed
      if (heroCover.complete) {
        runAnimation();
      } else {
        heroCover.addEventListener('load', runAnimation, { once: true });
      }
    }

    // Parallax (always, after a small delay to not conflict with entrance)
    setTimeout(() => {
      const cover = document.querySelector('.cs__hero-cover') as HTMLElement;
      if (cover) {
        gsap.to(cover, {
          yPercent: 15,
          ease: 'none',
          scrollTrigger: {
            trigger: hero,
            start: 'top top',
            end: 'bottom top',
            scrub: true,
          },
        });
      }
    }, raw ? 1000 : 0);
  }

  function initLightbox() {
    const mediaElements = document.querySelectorAll('.cs__body img, .cs__body video');

    // Auto-detect portrait images and add .cs-portrait class (skip ImageRow images)
    mediaElements.forEach((el) => {
      if (el.tagName === 'IMG') {
        const img = el as HTMLImageElement;
        if (img.closest('.image-row')) return;
        const classify = () => {
          if (img.naturalHeight > img.naturalWidth) {
            img.classList.add('cs-portrait');
          }
        };
        if (img.complete) classify();
        else img.addEventListener('load', classify);
      }
    });

    mediaElements.forEach((el) => {
      el.addEventListener('click', (e) => {
        e.stopPropagation();

        // Prevent body scroll
        document.body.style.overflow = 'hidden';

        const overlay = document.createElement('div');
        overlay.className = 'cs-lightbox';

        const closeBtn = document.createElement('button');
        closeBtn.className = 'cs-lightbox__close';
        closeBtn.innerHTML = '✕';
        closeBtn.setAttribute('aria-label', 'Close');
        overlay.appendChild(closeBtn);

        const isMobile = window.innerWidth <= 768;
        const hint = document.createElement('span');
        hint.className = 'cs-lightbox__hint';
        hint.textContent = isMobile ? 'Pinch to zoom · Tap to close' : 'Click anywhere to close';
        overlay.appendChild(hint);

        let clone: HTMLElement;
        if (el.tagName === 'VIDEO') {
          clone = document.createElement('video');
          (clone as HTMLVideoElement).src = (el as HTMLVideoElement).src;
          (clone as HTMLVideoElement).autoplay = true;
          (clone as HTMLVideoElement).loop = true;
          (clone as HTMLVideoElement).muted = false;
          (clone as HTMLVideoElement).playsInline = true;
          (clone as HTMLVideoElement).controls = true;
        } else {
          clone = document.createElement('img');
          (clone as HTMLImageElement).src = (el as HTMLImageElement).src;
          (clone as HTMLImageElement).alt = (el as HTMLImageElement).alt || '';
        }
        overlay.appendChild(clone);

        const close = () => {
          document.body.style.overflow = '';
          overlay.classList.remove('cs-lightbox--visible');
          setTimeout(() => overlay.remove(), 300);
        };

        closeBtn.addEventListener('click', (e) => { e.stopPropagation(); close(); });
        overlay.addEventListener('click', (e) => {
          if (e.target === overlay) close();
        });
        // Close on Escape key
        const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') { close(); window.removeEventListener('keydown', onKey); } };
        window.addEventListener('keydown', onKey);

        document.body.appendChild(overlay);
        requestAnimationFrame(() => overlay.classList.add('cs-lightbox--visible'));
      });
    });
  }

  function initCaseStudy() {
    initHeroTransition();
    initSectionNav();
    initLightbox();

    // Wrap tables for horizontal scroll on mobile
    document.querySelectorAll('.cs__body table').forEach((table) => {
      if (!table.parentElement?.classList.contains('table-wrap')) {
        const wrap = document.createElement('div');
        wrap.className = 'table-wrap';
        table.parentNode!.insertBefore(wrap, table);
        wrap.appendChild(table);
      }
    });

    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    const h2s = document.querySelectorAll('.cs__body h2');
    const imgs = document.querySelectorAll('.cs__body img:not(.image-row__img)');
    const csSections = document.querySelectorAll('[data-cs-section]');
    const bqs = document.querySelectorAll('.cs__body blockquote');
    const nextSection = document.querySelector('.cs__next');

    if (prefersReducedMotion) {
      gsap.set(h2s, { opacity: 1, y: 0 });
      gsap.set(imgs, { opacity: 1, y: 0, scale: 1 });
      gsap.set(csSections, { opacity: 1, y: 0 });
      gsap.set(bqs, { opacity: 1, x: 0 });
      if (nextSection) gsap.set(nextSection, { opacity: 1, y: 0 });
      return;
    }

    // Animate h2 headings
    h2s.forEach((h2) => {
      gsap.fromTo(h2,
        { opacity: 0, y: 24 },
        {
          opacity: 1, y: 0, duration: 0.6, ease: 'power3.out',
          scrollTrigger: { trigger: h2, start: 'top 80%', once: true },
        }
      );
    });

    // Animate images (skip ImageRow — those animate as a group via data-cs-section)
    imgs.forEach((img) => {
      gsap.fromTo(img,
        { opacity: 0, y: 32, scale: 0.98 },
        {
          opacity: 1, y: 0, scale: 1, duration: 0.7, ease: 'power3.out',
          scrollTrigger: { trigger: img, start: 'top 80%', once: true },
        }
      );
    });

    // Animate data-cs-section elements (MetricGrid, ProcessStep)
    csSections.forEach((el) => {
      gsap.fromTo(el,
        { opacity: 0, y: 24 },
        {
          opacity: 1, y: 0, duration: 0.6, ease: 'power3.out',
          scrollTrigger: { trigger: el, start: 'top 80%', once: true },
        }
      );
    });

    // Animate blockquotes
    bqs.forEach((bq) => {
      gsap.fromTo(bq,
        { opacity: 0, x: -16 },
        {
          opacity: 1, x: 0, duration: 0.6, ease: 'power3.out',
          scrollTrigger: { trigger: bq, start: 'top 80%', once: true },
        }
      );
    });

    // Next project card
    if (nextSection) {
      gsap.fromTo(nextSection,
        { opacity: 0, y: 30 },
        {
          opacity: 1, y: 0, duration: 0.65, ease: 'power3.out',
          scrollTrigger: { trigger: nextSection, start: 'top 80%', once: true },
        }
      );
    }
  }

  // Force deck button to do a hard navigation (bypass view transitions + smooth scroll)
  function initDeckBtn() {
    const deckBtn = document.querySelector('.cs__deck-btn');
    if (deckBtn) {
      deckBtn.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();
        window.location.href = (deckBtn as HTMLAnchorElement).href;
      });
    }
  }

  function initFluidCS() {
    document.querySelectorAll<HTMLElement>('[data-fluid-cs]').forEach((el) => {
      const hl = (el.querySelector('.pw-gate__back-highlight') || el.querySelector('.pw-gate__btn-highlight')) as HTMLElement;
      if (!hl) return;
      el.addEventListener('mouseenter', (e: MouseEvent) => {
        const rect = el.getBoundingClientRect();
        const cx = e.clientX - rect.left;
        const cy = e.clientY - rect.top;
        hl.style.clipPath = `circle(0% at ${cx}px ${cy}px)`;
        hl.style.opacity = '1';
        requestAnimationFrame(() => {
          hl.style.transition = 'clip-path 0.45s cubic-bezier(0.4, 0, 0.2, 1)';
          hl.style.clipPath = `circle(150% at ${cx}px ${cy}px)`;
        });
      });
      el.addEventListener('mouseleave', (e: MouseEvent) => {
        const rect = el.getBoundingClientRect();
        const cx = e.clientX - rect.left;
        const cy = e.clientY - rect.top;
        hl.style.transition = 'clip-path 0.35s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.15s ease 0.2s';
        hl.style.clipPath = `circle(0% at ${cx}px ${cy}px)`;
        setTimeout(() => { hl.style.opacity = '0'; }, 300);
      });
    });
  }

  initCaseStudy();
  initDeckBtn();
  initFluidCS();
  document.addEventListener('astro:after-swap', () => { initCaseStudy(); initDeckBtn(); initFluidCS(); });
</script>
